import 'package:flutter/material.dart';

class SynchronizationView extends StatelessWidget {
  const SynchronizationView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(key.toString()),
    );
  }
}
